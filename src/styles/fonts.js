import WebFont from 'webfontloader';

WebFont.load({
  google: {
    families: ['Barlow Condensed:300,500,600', 'sans-serif']
  }
});

const fonts = {
    font: 'Barlow Condensed, sans-serif',
    transformation: 'uppercase'
}

export default fonts;
