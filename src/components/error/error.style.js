import styled from 'styled-components';
import theme from '../../styles/theme';

export const StyledError= styled.div`
                      color: ${theme.errorFont};
                      display: flex;
                      justify-content: center;
                      min-height:20px;
                      `;